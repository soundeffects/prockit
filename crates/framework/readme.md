# Prockit Framework
A pipeline definition that allows composing procedural generation tools.

Better documentation is underway.

## TODO
- Doc comments and examples in `plugins.rs`
- Make `Provides` work with currying and `with` patterns
